# Session Snapshot — 2026-08-19 14:56

## Session Goal
Evaluate an external architecture review of `bfish` and identify recommendations worth carrying into implementation without changing the authoritative project design yet.

## Status
Review complete and recommendations triaged; no architecture or source files were changed, and scaffolding has not started.

## Accomplished This Session
- Inspected the repository and confirmed the review's claimed `claude/readme-review-2026-08-18.md` file is not present; the pasted conversation text is the only available copy.
- Verified Apple's requirement for `NSAudioCaptureUsageDescription` when using Core Audio process taps and confirmed microphone capture uses its own permission description.
- Identified stable code identity, embedded usage descriptions, signing diagnostics, and supported permission probes as milestone-one concerns for live capture.
- Agreed that Ollama should return only translation-owned fields such as `english_text`; source text remains immutable application data and should not be echoed through the model.
- Agreed to set Ollama `keep_alive` explicitly and measure cold-load latency separately from warm utterance latency.
- Confirmed Argmax recommends `large-v3-v20240930_626MB` for maximum multilingual accuracy and `tiny` for fast development; retained benchmarking rather than declaring a production model before evidence.
- Identified session language latching with hysteresis and explicit overrides as preferable to independent detection on every short utterance.
- Corrected the language-model assumption: Whisper exposes Portuguese (`pt`), not a distinct automatically detected Brazilian Portuguese token; `pt-BR` must come from user/session configuration if needed.
- Agreed to ship VAD-bounded utterances without overlap first and add timestamp-aligned overlap only when fixtures demonstrate a need.
- Promoted VAD, transcription quality gating, and hallucination filtering as named implementation concerns; cautioned against unconditional phrase blocklists.
- Recommended per-stage JSON Lines diagnostics on stderr and golden fixtures for segmentation, silence, music, laughter, overlapping speech, interruptions, and translation failure.
- Verified Apple SpeechTranscriber exposes runtime availability and locale discovery and is worth a bounded alternate-recognizer spike.
- Verified Apple TranslationSession now supports direct initialization in non-UI contexts for already-installed language pairs and belongs in the translation scoreboard as an alternate `TextTranslating` implementation.
- Clarified that the terminal CLI should be both a supported interface and the validation/automation harness for a later menu-bar or overlay application.

## In Progress
- No application work is in progress.
- This session snapshot and `.session/INDEX.md` are being committed and pushed.

## Pending / Blocked
- Decide which review recommendations should be promoted into `README.md` before scaffolding; none were applied automatically because the user requested evaluation rather than changes.
- Resolve the prior pending decision on whether offline SpeakerKit diarization is a baseline requirement.
- Install/select full Xcode and verify Ollama service/model state.
- Scaffold the Swift package, domain types, metrics events, and `bfish doctor`.
- Determine the supported mechanism for checking audio-capture and microphone permission without reading private TCC state.
- Spike Apple SpeechTranscriber locale coverage and headless Apple TranslationSession after the primary file-based path works.

## Key Decisions
- Carry stable executable identity and permission diagnostics into implementation planning, while avoiding the unverified absolute claim that every Core Audio tap executable must be conventionally signed merely to function.
- Do not round-trip source text through Ollama; attach returned English translation to the authoritative source segment locally.
- Use `tiny` for plumbing and treat Argmax's large-v3 model as the first serious accuracy baseline, while retaining the model scoreboard.
- Start live segmentation without overlap to bound scope; introduce overlap only through a fixture-driven change.
- Treat suppression as evidence-based quality gating rather than silently deleting every occurrence of known Whisper hallucination phrases.
- Add Apple SpeechTranscriber and Apple TranslationSession as benchmark candidates, not defaults.
- Keep the CLI as a real supported product surface even if a menu-bar/overlay UI becomes the preferred live-caption experience.

## Important Context
- `README.md` remains unchanged and authoritative for the current accepted architecture.
- The pasted review is useful but not stored in the repository despite its claim; do not assume `claude/readme-review-2026-08-18.md` exists.
- Apple documents that Core Audio taps require an `NSAudioCaptureUsageDescription` key and that first capture prompts for system audio recording permission.
- Stable signing identity matters operationally because TCC grants are associated with code identity; exact development packaging behavior must be tested on the target host.
- TranslationSession's headless initializer requires source and target languages already installed on the device.
- SpeechTranscriber language support must be queried through its runtime locale APIs on the actual Mac.

## Tool & Dependency Requirements
- Existing tool requirements remain documented in `README.md` and prior session snapshots.
- Future live-capture builds need an embedded Info.plist with system-audio and microphone usage descriptions plus a stable local signing/package process.
- Future alternate-engine spikes require macOS 26 Speech and Translation frameworks and installed locale assets.

## Git State
- Branch: `main`, tracking `origin/main`.
- Uncommitted changes before this checkpoint commit: new `.session/2026-08-19_14-56.md` and modified `.session/INDEX.md` only.
- Recent commits: `4398e26 Define offline model evaluation scoreboard`; `e8c6356 Document architecture and prior art`; `7634b9f Initialize bfish project documentation`.

## Next Steps (Priority Order)
1. Decide whether SpeakerKit offline diarization is a baseline requirement and whether to promote the accepted review recommendations into the README.
2. Install/select full Xcode and verify Ollama service plus installed models.
3. Scaffold `BFishCore`, the `bfish` CLI, tests, an embedded Info.plist/signing workflow, JSONL metrics types, and `bfish doctor`.
4. Build the file-based WhisperKit-to-Ollama proof of concept using immutable source text and translation-only model output.
5. Add VAD/quality gates and golden fixtures before implementing overlap.
6. Run bounded spikes for Apple SpeechTranscriber and headless Apple TranslationSession and record them in the existing scoreboards.

## Resume Prompt
Resume `bfish` by reading `README.md`, `.session/INDEX.md`, and `.session/2026-08-19_14-56.md`. An external review was triaged without changing the README. High-value implementation recommendations include stable signing and TCC diagnostics, translation-only Ollama responses with explicit keep-alive, per-stage JSONL metrics, VAD/quality gates, no-overlap initial segmentation, language latching, and benchmark spikes for Apple SpeechTranscriber and headless TranslationSession. Resolve the pending SpeakerKit baseline decision, then verify Xcode/Ollama and scaffold the Swift package.
