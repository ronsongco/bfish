# Benchmarks

This directory will contain versioned benchmark fixtures, prompt templates, raw run metadata, and generated scoreboards.

Raw run output and generated scoreboards are ignored by Git until their schemas and provenance rules are established. See the project README for the planned translation-only and end-to-end evaluation tracks.
