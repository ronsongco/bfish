/// Reusable domain and pipeline components for bfish.
public enum BFishCore {
    /// Canonical application version. Packaging derives its plist value by
    /// invoking the built executable, so no deploy-time resource is required.
    public static let version = "0.1.0"
}
